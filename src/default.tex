\documentclass[
  paper=a4,
  fontsize=11pt,
  twoside=true,
  headings=normal,
  DIV=12
]{scrbook}

% ------------------------------------------------------------
% Engine check: Pandoc must use LuaLaTeX
% ------------------------------------------------------------
\usepackage{ifluatex}
\ifluatex\else
  \errmessage{This template requires LuaLaTeX. Use --pdf-engine=lualatex}
\fi

% ------------------------------------------------------------
% Font setup (TeX Gyre Pagella = Palatino clone)
% ------------------------------------------------------------
\usepackage{fontspec}
\defaultfontfeatures{Ligatures=TeX, Scale=MatchLowercase}

\setmainfont{TeX Gyre Pagella}
\setsansfont{TeX Gyre Heros}
\setmonofont{TeX Gyre Cursor}

% ------------------------------------------------------------
% Page layout
% ------------------------------------------------------------
\usepackage{geometry}
\geometry{
  paper=a4paper,
  margin=25mm,
  headheight=14pt
}

% ------------------------------------------------------------
% Microtypography
% ------------------------------------------------------------
\usepackage{microtype}

% ------------------------------------------------------------
% Pandoc inserts its own packages here
% ------------------------------------------------------------
$if(highlighting-macros)$
$highlighting-macros$
$endif$

$if(tables)$
\usepackage{longtable,booktabs}
$endif$

$if(graphics)$
\usepackage{graphicx}
$endif$

% ------------------------------------------------------------
% Document metadata
% ------------------------------------------------------------
\title{$title$}
\author{$author$}
\date{$date$}

% ------------------------------------------------------------
% Begin document
% ------------------------------------------------------------
\begin{document}

\frontmatter
\maketitle
\tableofcontents

\mainmatter
$body$

\end{document}
